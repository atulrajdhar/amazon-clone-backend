const expect = require('chai').expect;

describe('checkForPassword', function () {
    const validatePassword = require('../helpers').validatePassword;
    it('should contain at least 8 characters', function () {
        
    });
});